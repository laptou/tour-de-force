{
    "files.exclude": {
        "rain-library-client/dist": true,
        "**/.cache-loader": true,
        "**/*.js.map": true,
        "**/*.js": {"when": "$(basename).ts"},
        "**/*.d.ts": {"when": "$(basename).ts"}
    }
}